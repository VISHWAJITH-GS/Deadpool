# Lightweight Memory

Use SQLite for explicit information:
- app aliases
- website aliases
- project locations
- user-defined workflows
- preferences

Example:
"coding" → open VS Code + LeetCode
"TCE Connect" → D:\Projects\TCE-Connect
"browser" → Chrome

Do not automatically store passwords, API keys, authentication tokens or credentials.
